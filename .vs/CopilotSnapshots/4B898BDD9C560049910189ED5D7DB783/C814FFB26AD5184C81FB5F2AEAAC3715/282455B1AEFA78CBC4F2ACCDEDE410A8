:root {
    --ivory: #F6F4EF;
    --paper: #FBFAF7;
    --ink: #15130F;
    --ink-soft: #3A362E;
    --gold: #A9863C;
    --gold-deep: #8C6A2F;
    --gold-pale: #D9C596;
    --line: #E3DECF;
    --grey: #8B8578;
}

* {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
}

body {
    font-family: 'Jost',sans-serif;
    background: var(--paper);
    color: var(--ink);
    -webkit-font-smoothing: antialiased;
}

/*  Utility bar */
.utility-bar {
    background: var(--ink);
    color: var(--gold-pale);
    font-size: 11px;
    letter-spacing: .12em;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 8px 40px;
    text-transform: uppercase;
}

    .utility-bar .left, .utility-bar .right {
        display: flex;
        gap: 24px;
        align-items: center;
    }

    .utility-bar a {
        color: var(--gold-pale);
        text-decoration: none;
        opacity: .85;
        transition: opacity .2s;
    }

        .utility-bar a:hover {
            opacity: 1;
            color: #fff;
        }

/*  Header */
header {
    background: #fff;
    border-bottom: 1px solid var(--line);
    position: relative;
    z-index: 50;
}

.header-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 22px 40px 18px;
}

.header-side {
    display: flex;
    align-items: center;
    gap: 20px;
    width: 220px;
}

    .header-side.right {
        justify-content: flex-end;
    }

.icon-btn {
    background: none;
    border: none;
    cursor: pointer;
    color: var(--ink);
    display: flex;
    align-items: center;
    justify-content: center;
    width: 34px;
    height: 34px;
    transition: color .2s, transform .2s;
}

    .icon-btn:hover {
        color: var(--gold-deep);
    }

.search-wrap {
    position: relative;
    display: flex;
    align-items: center;
}

    .search-wrap input {
        border: none;
        background: transparent;
        border-bottom: 1px solid transparent;
        font-family: 'Jost',sans-serif;
        font-size: 12.5px;
        letter-spacing: .04em;
        padding: 4px 2px;
        width: 0;
        opacity: 0;
        transition: width .35s ease, opacity .35s ease, border-color .35s;
        color: var(--ink);
    }

    .search-wrap.open input {
        width: 150px;
        opacity: 1;
        border-bottom: 1px solid var(--gold-deep);
    }

    .search-wrap input::placeholder {
        color: var(--grey);
    }

    .search-wrap input:focus {
        outline: none;
    }

/* Logo */
.brand {
    text-align: center;
    text-decoration: none;
    color: var(--ink);
    line-height: 1;
}

    .brand .mark {
        font-family: 'Cormorant Garamond', serif;
        font-size: 30px;
        font-weight: 600;
        letter-spacing: .16em;
        display: block;
    }

        .brand .mark span {
            color: var(--ink);
        }

    .brand .sub {
        display: block;
        margin-top: 5px;
        font-size: 9.5px;
        letter-spacing: .5em;
        color: var(--grey);
        text-transform: uppercase;
    }

.cart-count {
    position: absolute;
    top: -4px;
    right: -6px;
    background: var(--gold-deep);
    color: #fff;
    font-size: 9px;
    width: 15px;
    height: 15px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: 'Jost',sans-serif;
}

.icon-btn-wrap {
    position: relative;
}

/* ===== Nav ===== */
nav.main-nav {
    display: flex;
    justify-content: center;
    border-top: 1px solid var(--line);
}

    nav.main-nav ul {
        list-style: none;
        display: flex;
        gap: 52px;
    }

    nav.main-nav li {
        position: relative;
    }

    nav.main-nav .nav-link {
        display: block;
        padding: 16px 2px 14px;
        color: var(--ink-soft);
        text-decoration: none;
        font-size: 12.5px;
        letter-spacing: .16em;
        text-transform: uppercase;
        position: relative;
        transition: color .25s;
    }

        nav.main-nav .nav-link::after {
            content: "";
            position: absolute;
            left: 0;
            right: 0;
            bottom: 8px;
            height: 1px;
            background: var(--gold-deep);
            transform: scaleX(0);
            transform-origin: center;
            transition: transform .3s ease;
        }

    nav.main-nav li:hover .nav-link,
    nav.main-nav li.active .nav-link {
        color: var(--ink);
    }

        nav.main-nav li:hover .nav-link::after,
        nav.main-nav li.active .nav-link::after {
            transform: scaleX(1);
        }

.nav-logout-form {
    display: inline;
    margin: 0;
    padding: 0;
}

    .nav-logout-form .logout-link {
        border: none;
        background: none;
        font: inherit;
        cursor: pointer;
    }

.mega {
    position: absolute;
    top: 100%;
    left: 50%;
    transform: translate(-50%, 8px);
    background: var(--paper);
    border: 1px solid var(--line);
    box-shadow: 0 18px 40px rgba(21,19,15,.10);
    padding: 32px 36px;
    display: grid;
    grid-auto-flow: column;
    gap: 34px;
    opacity: 0;
    visibility: hidden;
    pointer-events: none;
    transition: opacity .28s ease, transform .28s ease;
    z-index: 60;
}

li:hover .mega {
    opacity: 1;
    visibility: visible;
    pointer-events: auto;
    transform: translate(-50%, 0);
}

.mega-item {
    width: 150px;
    text-align: center;
    text-decoration: none;
    color: var(--ink-soft);
}

    .mega-item .thumb {
        width: 150px;
        height: 150px;
        background: #F1EDE3;
        border: 1px solid var(--line);
        border-radius: 2px;
        display: flex;
        align-items: center;
        justify-content: center;
        margin-bottom: 12px;
        transition: border-color .25s, background .25s;
        overflow: hidden;
    }

        .mega-item .thumb img {
            width: 100%;
            height: 100%;
            object-fit: contain;
            display: block;
        }

    .mega-item:hover .thumb {
        border-color: var(--gold-deep);
        background: #EFE9D9;
    }

    .mega-item .name {
        font-size: 11.5px;
        letter-spacing: .1em;
        text-transform: uppercase;
        color: var(--ink);
    }

    .mega-item .meta {
        display: block;
        margin-top: 2px;
        font-size: 10px;
        color: var(--grey);
        letter-spacing: .04em;
    }

.mega-foot {
    grid-column: 1/-1;
    display: flex;
    justify-content: center;
    padding-top: 6px;
    border-top: 1px solid var(--line);
    margin-top: 4px;
}

    .mega-foot a {
        font-size: 11px;
        letter-spacing: .14em;
        text-transform: uppercase;
        color: var(--gold-deep);
        text-decoration: none;
        border-bottom: 1px solid var(--gold-deep);
        padding-bottom: 2px;
    }

@media (max-width: 900px) {
    .header-side {
        width: auto;
    }

    .brand .mark {
        font-size: 22px;
    }

    nav.main-nav ul {
        gap: 26px;
    }

    .mega {
        left: 0;
        transform: translate(0,8px);
        grid-auto-flow: row;
        grid-template-columns: repeat(3,1fr);
        width: 90vw;
    }
}

/* ===== Hero ===== */
.hero {
    position: relative;
    height: 88vh;
    min-height: 520px;
    overflow: hidden;
    display: flex;
    align-items: flex-end;
}

.hero-media {
    position: absolute;
    inset: 0;
    z-index: 0;
}

    .hero-media img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        object-position: center;
        display: block;
    }

.hero-overlay {
    position: absolute;
    inset: 0;
    z-index: 1;
    background: linear-gradient(0deg, rgba(21,19,15,.78) 0%, rgba(21,19,15,.35) 45%, rgba(21,19,15,.1) 100%);
}

.hero-content {
    position: relative;
    z-index: 2;
    padding: 0 60px 72px;
    max-width: 720px;
    color: var(--paper);
}

    .hero-content h1 {
        font-family: 'Cormorant Garamond', serif;
        font-weight: 600;
        font-size: 56px;
        letter-spacing: .04em;
        line-height: 1.1;
        margin-bottom: 18px;
        text-transform: uppercase;
    }

    .hero-content p {
        font-family: 'Jost', sans-serif;
        font-weight: 300;
        font-size: 14.5px;
        line-height: 1.7;
        letter-spacing: .02em;
        color: #EDE8DC;
        max-width: 520px;
        margin-bottom: 32px;
    }

.hero-btn {
    font-family: 'Jost', sans-serif;
    background: var(--gold-deep);
    color: var(--ink);
    border: none;
    padding: 15px 38px;
    font-size: 12px;
    letter-spacing: .16em;
    text-transform: uppercase;
    cursor: pointer;
    transition: background .25s ease, color .25s ease;
}

    .hero-btn:hover {
        background: var(--paper);
        color: var(--ink);
    }

@media (max-width: 640px) {
    .hero-content {
        padding: 0 24px 48px;
    }

        .hero-content h1 {
            font-size: 36px;
        }
}



/*feature section show*/
.featured {
    max-width: 1100px;
    margin: 60px auto;
    padding: 0 40px;
}

    .featured h2 {
        font-size: 28px;
        font-weight: 700;
        margin-bottom: 24px;
        color: #1a1a1a;
    }

.featured-card {
    display: flex;
    gap: 40px;
    flex-wrap: wrap;
    align-items: stretch;
}

    .featured-card img {
        width: 100%;
        max-width: 560px;
        height: 380px;
        object-fit: cover;
        object-position: center;
        border-radius: 10px;
        flex: 1 1 480px;
    }

.featured-info {
    flex: 1 1 320px;
    display: flex;
    flex-direction: column;
    justify-content: center;
}

.badge {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    background: black;
    color: white;
    font-size: 12px;
    font-weight: 600;
    letter-spacing: 0.5px;
    padding: 6px 12px;
    border-radius: 4px;
    width: fit-content;
    margin-bottom: 14px;
}

.featured-info h3 {
    font-size: 30px;
    color: #3b5fe0;
    margin-bottom: 8px;
}

.featured-info .developer {
    color: #3b5fe0;
    font-size: 16px;
    margin-bottom: 8px;
}

.featured-info .location {
    font-size: 14px;
    font-weight: 600;
    color: #333;
    margin-bottom: 20px;
}

.divider {
    border: none;
    border-top: 2px dashed #ddd;
    margin-bottom: 20px;
}

.property-type {
    color: #3b5fe0;
    font-weight: 700;
    font-size: 18px;
    margin-bottom: 6px;
}

.price {
    font-size: 22px;
    font-weight: 700;
    color: #1a1a1a;
    margin-bottom: 18px;
}

.specs {
    display: flex;
    gap: 32px;
}

    .specs div {
        text-align: center;
        font-size: 14px;
        color: #555;
    }

    .specs strong {
        display: block;
        font-size: 18px;
        color: #1a1a1a;
        margin-bottom: 2px;
    }

.hero-panel {
    background: rgba(255, 255, 255, 0.85);
    backdrop-filter: blur(2px);
    width: 100%;
    max-width: 900px;
    padding: 40px 48px;
    border-radius: 8px;
    box-shadow: 0 8px 30px rgba(0,0,0,0.2);
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
}

    .hero-panel h1 {
        font-size: 40px;
        font-weight: 800;
        line-height: 1.2;
        color: darkblue;
        text-shadow: none;
        margin-bottom: 28px;
    }

.hero-tabs {
    display: flex;
    gap: 56px;
    border-bottom: 2px solid white;
    justify-content: center;
    width: 100%;
}

    .hero-tabs a {
        text-decoration: none;
        font-size: 32px;
        font-weight: 800;
        color: #6f8fe8;
        padding-bottom: 14px;
        border-bottom: 4px solid transparent;
        transition: color 0.2s ease, border-color 0.2s ease;
    }

        .hero-tabs a:hover,
        .hero-tabs a:focus-visible {
            color: orange;
            border-bottom-color: orange;
        }

.properties {
    max-width: 1300px;
    margin: 60px auto;
    padding: 0 40px;
}

    .properties h2 {
        font-size: 28px;
        font-weight: 700;
        color: black;
        margin-bottom: 24px;
    }

.property-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 24px;
}

@media (max-width:1000px) {
    .property-grid {
        grid-template-columns: repeat(2, 1fr);
    }
}

@media (max-width:600px) {
    .property-grid {
        grid-template-columns: 1fr;
    }
}

.property-card {
    display: block;
    text-decoration: none;
    color: inherit;
    border: 1px solid #e2e2e2;
    border-radius: 6px;
    overflow: hidden;
    transition: box-shadow 0.2s ease, transform 0.2s ease;
}

    .property-card:hover {
        box-shadow: 0 8px 20px rgba(0,0,0,0.12);
        transform: translateY(-3px);
    }

    .property-card .thumb-wrap {
        position: relative;
    }

    .property-card img {
        width: 100%;
        height: 220px;
        object-fit: cover;
        object-position: center;
        display: block;
    }

.featured-badge {
    position: absolute;
    top: 14px;
    left: 14px;
    background: #fff;
    color: #1a1a1a;
    font-size: 13px;
    font-weight: 700;
    padding: 6px 12px;
    border-radius: 20px;
    display: flex;
    align-items: center;
    gap: 6px;
    box-shadow: 0 2px 6px rgba(0,0,0,0.15);
}

.property-card .card-body {
    padding: 16px;
}

.property-card .price {
    color: #3b5fe0;
    font-size: 20px;
    font-weight: 700;
    margin-bottom: 8px;
}

.property-card .title {
    font-size: 16px;
    font-weight: 700;
    color: #1a1a1a;
    margin-bottom: 8px;
    line-height: 1.35;
}

.property-card .location {
    font-size: 13px;
    color: #777;
    margin-bottom: 10px;
    line-height: 1.4;
}

.property-card .location {
    font-size: 13px;
    color: #777;
    margin-bottom: 10px;
    line-height: 1.4;
}

.property-card .sqm {
    font-size: 14px;
    font-weight: 700;
    color: #1a1a1a;
    margin-bottom: 6px;
}

.property-card .by {
    font-size: 13px;
    color: #888;
}

.detail-wrap {
    max-width: 1100px;
    margin: 50px auto;
    padding: 0 40px;
}

.back-link {
    display: inline-block;
    margin-bottom: 20px;
    color: #3b5fe0;
    text-decoration: none;
    font-weight: 600;
}

.detail-wrap .main-image {
    width: 100%;
    max-height: 520px;
    object-fit: cover;
    object-position: center;
    border-radius: 8px;
    margin-bottom: 30px;
}

.detail-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    flex-wrap: wrap;
    gap: 20px;
    margin-bottom: 20px;
}

    .detail-header h1 {
        font-size: 28px;
        color: #1a1a1a;
        max-width: 650px;
    }

    .detail-header .price {
        font-size: 26px;
        font-weight: 800;
        color: #3b5fe0;
    }

.detail-specs {
    display: flex;
    flex-wrap: wrap;
    gap: 32px;
    padding: 24px 0;
    border-top: 1px solid #eee;
    border-bottom: 1px solid #eee;
    margin-bottom: 30px;
}

    .detail-specs div {
        min-width: 140px;
    }

    .detail-specs span {
        display: block;
        font-size: 13px;
        color: #888;
        margin-bottom: 4px;
    }

    .detail-specs strong {
        font-size: 17px;
        color: #1a1a1a;
    }

.detail-desc h2 {
    font-size: 20px;
    margin-bottom: 12px;
    color: #1a1a1a;
}

.detail-desc p {
    font-size: 15px;
    line-height: 1.7;
    color: #444;
    margin-bottom: 30px;
}

.agent-card {
    background: #f7f8fa;
    border-radius: 8px;
    padding: 24px 28px;
    max-width: 420px;
}

    .agent-card h3 {
        font-size: 18px;
        margin-bottom: 6px;
        color: #1a1a1a;
    }

    .agent-card .agent-role {
        font-size: 14px;
        color: #777;
        margin-bottom: 14px;
    }

    .agent-card p.agent-desc {
        font-size: 14px;
        color: #555;
        line-height: 1.6;
        margin-bottom: 16px;
    }

    .agent-card .contact-row {
        font-size: 14px;
        color: #333;
        margin-bottom: 8px;
    }

        .agent-card .contact-row strong {
            display: inline-block;
            width: 70px;
            color: #1a1a1a;
        }

.property-grid-2 {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 24px;
    max-width: 760px;
}

@media (max-width:600px) {
    .property-grid-2 {
        grid-template-columns: 1fr;
    }
}

.properties + .properties {
    margin-top: 0;
}

.featured-card-link {
    display: block;
    text-decoration: none;
    color: inherit;
    border-radius: 10px;
    transition: box-shadow 0.2s ease, transform 0.2s ease;
}

    .featured-card-link:hover {
        box-shadow: 0 12px 30px rgba(0,0,0,0.15);
        transform: translateY(-4px);
    }

.featured-list {
    display: flex;
    flex-direction: column;
    gap: 40px;
}




/* footer */
.site-footer {
    margin-top: 60px;
    background: #1a2b6d;
    color: #dfe3f2;
    padding: 60px 40px 0;
}

.footer-top {
    max-width: 1200px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1.4fr 1fr 1.4fr;
    gap: 50px;
    padding-bottom: 44px;
}

@media (max-width: 800px) {
    .footer-top {
        grid-template-columns: 1fr;
        gap: 36px;
    }
}

.footer-logo {
    font-size: 24px;
    font-weight: bold;
    color: #fff;
    margin-bottom: 14px;
}

    .footer-logo span {
        color: #f7931e;
    }

.footer-brand p {
    font-size: 14px;
    line-height: 1.7;
    color: #b7bede;
    max-width: 320px;
}

.footer-links h4,
.footer-contact h4 {
    color: #fff;
    font-size: 15px;
    font-weight: 700;
    letter-spacing: 0.3px;
    margin-bottom: 18px;
}

.footer-links {
    display: flex;
    flex-direction: column;
}

.footer-links a {
    color: #b7bede;
    text-decoration: none;
    font-size: 14px;
    margin-bottom: 12px;
    width: fit-content;
    transition: color 0.2s ease;
}

.footer-links a:hover {
    color: #f7931e;
}

.footer-contact-name {
    color: #fff;
    font-size: 15px;
    font-weight: 600;
    margin-bottom: 8px;
}

.footer-contact-desc {
    font-size: 14px;
    line-height: 1.7;
    color: #b7bede;
    margin-bottom: 16px;
    max-width: 340px;
}

.footer-contact-row {
    font-size: 14px;
    color: #dfe3f2;
    margin-bottom: 8px;
}

.footer-bottom {
    border-top: 1px solid rgba(255,255,255,0.12);
    padding: 20px 0;
    text-align: center;
}

    .footer-bottom p {
        font-size: 13px;
        color: #8f96bc;
    }
